#Conexión de SOLO LECTURA a la misma Postgres que usa gestorBQ — ver SPK-SG2 en el backlog:
#el usuario de este servicio debe crearse acotado (sin INSERT/UPDATE/DELETE), nunca reusar el de la app.
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
